var canvas;

function setup() {
	canvas = createCanvas(400, 400);

	canvas.style('z-index', '-1');
	canvas.position(100, 100);
}

function draw() {

	background(51);
	fill(0);
	rect(200, 200, 200, 200);

}